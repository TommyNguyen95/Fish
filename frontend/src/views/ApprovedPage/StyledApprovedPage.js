import styled from 'styled-components';

const StyledText = styled.h3`
  color: white;
  font-size: 25px;
  text-align: center;
`;

const StyledIcon = styled.p`
  text-align: center;
  font-size: 60px;
`;

export {
  StyledText,
  StyledIcon
}